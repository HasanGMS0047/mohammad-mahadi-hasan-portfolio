# Assets

Drop general-purpose files here (logos, extra icons, PDFs, etc.) — anything referenced
from the site as `/assets/<filename>`.

This is separate from `public/images/`, which holds the profile photo and the generated
project thumbnails already wired into the site. Use this folder for anything new.
